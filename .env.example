# ============================================================================
# PaidSearchNav MCP - Environment Configuration Example
# ============================================================================
#
# ⚠️  SECURITY WARNINGS:
#
# 1. NEVER commit your actual .env file to version control
# 2. This is an EXAMPLE file only - copy it to .env and fill in real values
# 3. Add .env to .gitignore (already configured)
# 4. Keep credentials secure and never share them
# 5. Rotate credentials regularly (every 90 days recommended)
# 6. Use different credentials for development, staging, and production
#
# ============================================================================

# Environment Configuration
ENVIRONMENT=development

# Google Ads API Configuration
# Obtain these credentials from: https://console.cloud.google.com/
# Follow setup guide: docs/GOOGLE_ADS_SETUP.md

# ⚠️  DO NOT COMMIT REAL VALUES - These are examples only!
GOOGLE_ADS_DEVELOPER_TOKEN=your_dev_token_here  # From Google Ads API Center
GOOGLE_ADS_CLIENT_ID=your_client_id_here.apps.googleusercontent.com
GOOGLE_ADS_CLIENT_SECRET=your_client_secret_here  # Keep this secret!
GOOGLE_ADS_REFRESH_TOKEN=your_refresh_token_here  # Keep this secret!
GOOGLE_ADS_LOGIN_CUSTOMER_ID=1234567890  # 10 digits, no dashes
GOOGLE_ADS_API_VERSION=v17  # Or v18 for latest features

# Google Cloud Platform Configuration
GOOGLE_APPLICATION_CREDENTIALS=/app/credentials/service-account.json
GCP_PROJECT_ID=your-gcp-project-id

# Redis Configuration (Optional - for caching)
# Improves performance by reducing API calls
REDIS_URL=redis://localhost:6379/0  # Local Redis instance
REDIS_TTL=3600  # Cache time-to-live in seconds (default: 1 hour)

# MCP Server Configuration
MCP_PORT=8080
MCP_HOST=0.0.0.0

# ============================================================================
# SECURITY BEST PRACTICES:
#
# 1. Use a password manager to generate and store credentials
# 2. Enable 2FA on your Google account
# 3. Review Google Cloud audit logs regularly
# 4. Set up alerts for unusual API usage
# 5. Use separate service accounts for different environments
# 6. Follow principle of least privilege for API scopes
# 7. Monitor for credential leaks using tools like git-secrets
# 8. Never log credentials or tokens in application logs
# 9. Ensure production environment uses INFO or higher log level
# 10. Rotate service account keys and refresh tokens every 90 days
# ============================================================================
